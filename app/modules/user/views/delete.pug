extends ../../../templates/master.pug



block content

    h3.title List of Dog(s):
    ol
        //- This is how we iterate through arrays and access
        //- their elements
        each user in users
            li= `${user.name}`
            
    hr
    hr
    form(method="POST" action="/users/delete")
        .field
            .control
                input.input(type="text" placeholder="type the name you want to delete" name="name")     
                
            
        input(type="submit" value="delete")